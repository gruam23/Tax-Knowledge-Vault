# 适合转成输出的页面

```dataview
TABLE type, field, jurisdictions, status, career_use, updated
FROM "wiki"
WHERE contains(career_use, "interview") OR contains(career_use, "memo") OR contains(career_use, "presentation")
SORT status ASC, field ASC, file.name ASC
```
